stages:
  - lint
  - format
  - type_check
  - test
  - coverage

before_script:
  - pip install uv

lint:
  stage: lint
  image: python:3.12
  script:
    - uvx ruff check .

format:
  stage: format
  image: python:3.12
  script:
    - uvx ruff format --check .

type_check:
  stage: type_check
  image: python:3.12
  script:
    - uvx mypy .

test:
  stage: test
  image: python:3.12
  before_script:
    - pip install uv
    - uv sync
  script:
    - uv run pytest

coverage:
  stage: coverage
  image: python:3.12
  before_script:
    - pip install uv
    - uv sync
  script:
    - uv run pytest --cov=. --cov-report=xml